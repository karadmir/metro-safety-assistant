#include <opencv2/core/types.hpp>
#include "types.h"

extern "C" void process(cv::Mat input, std::vector<Person> people, Train train, SafetyLine safety_line, cv::Mat output) {
    // Do nothing
}

int main() {
    return 0;
}